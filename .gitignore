# Dependencies
node_modules/

# Build output
app/dist/
app/coverage/
client/.next/
client/coverage/
client/next-env.d.ts
*.tsbuildinfo

# Env
.env

# Self-signed TLS certs (generated locally by nginx/certs/generate-self-signed-cert.sh)
nginx/certs/*.pem

# Local MCP server config (see .mcp.json's own header comment - no
# secrets/machine paths in it, but it's documented as gitignored)
.mcp.json

# Logs
npm-debug.log*
*.stackdump

# OS
.DS_Store
Thumbs.db
